package dev.banhammer.plugin.database.model;

/**
 * Types of punishments that can be issued.
 *
 * @since 3.0.0
 */
public enum PunishmentType {
    BAN,
    TEMP_BAN,
    KICK,
    MUTE,
    TEMP_MUTE,
    JAIL,
    REGION_BAN,
    IP_BAN,
    WARNING
}
